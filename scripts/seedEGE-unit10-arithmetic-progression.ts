// scripts/seedEGE-unit10-arithmetic-progression.ts
//
// Unit 10 курса "ЕГЭ Математика Профиль" — текстовые задачи на
// арифметическую прогрессию, 9 задач с
// https://math-ege.sdamgia.ru/test?theme=89, один урок.

import postgres from 'postgres';
import { drizzle } from 'drizzle-orm/postgres-js';
import 'dotenv/config';
import * as schema from '../db/schema';

const queryClient = postgres(process.env.DATABASE_URL!);
const db = drizzle(queryClient, { schema });

const UNIT_ID = 96;
const AUTHOR = 'ЕГЭ Математика Профиль';

type ChallengeSeed = { question: string; correct: string; distractors: string[]; image?: string };

const challenges: ChallengeSeed[] = [
    { question: 'Бригада маляров красит забор длиной 240 метров, ежедневно увеличивая норму покраски на одно и то же число метров. Известно, что за первый и последний день в сумме бригада покрасила 60 метров забора. Определите, сколько дней бригада маляров красила весь забор.',
        correct: '8', distractors: ['16', '4', '9', '7', '12'] },
    { question: 'Бригада маляров красит забор длиной 630 метров, ежедневно увеличивая норму покраски на одно и то же число метров. Известно, что за первый и последний день в сумме бригада покрасила 140 метров забора. Определите, сколько дней бригада маляров красила весь забор.',
        correct: '9', distractors: ['18', '4,5', '10', '8', '13,5'] },
    { question: 'Рабочие прокладывают тоннель длиной 500 метров, ежедневно увеличивая норму прокладки на одно и то же число метров. Известно, что за первый день рабочие проложили 3 метра тоннеля. Определите, сколько метров тоннеля проложили рабочие в последний день, если вся работа была выполнена за 10 дней.',
        correct: '97', distractors: ['194', '48,5', '98', '96', '145,5'] },
    { question: 'Васе надо решить 434 задачи. Ежедневно он решает на одно и то же количество задач больше по сравнению с предыдущим днём. Известно, что за первый день Вася решил 5 задач. Определите, сколько задач решил Вася в последний день, если со всеми задачами он справился за 14 дней.',
        correct: '57', distractors: ['114', '28,5', '58', '56', '85,5'] },
    { question: 'Игорю надо решить 240 задач. Ежедневно он решает на одно и то же количество задач больше по сравнению с предыдущим днём. Известно, что за первый день Игорь решил 9 задач. Определите, сколько задач решил Игорь в последний день, если со всеми задачами он справился за 12 дней.',
        correct: '31', distractors: ['62', '15,5', '32', '30', '46,5'] },
    { question: 'Турист идёт из одного города в другой, каждый день проходя больше, чем в предыдущий день, на одно и то же расстояние. Известно, что за первый день турист прошёл 10 километров. Определите, сколько километров прошёл турист за третий день, если весь путь он прошёл за 6 дней, а расстояние между городами составляет 120 километров.',
        correct: '18', distractors: ['36', '9', '19', '17', '27'] },
    { question: 'Турист идёт из одного города в другой, каждый день проходя больше, чем в предыдущий день, на одно и то же расстояние. Известно, что за первый день турист прошёл 12 километров. Определите, сколько километров прошёл турист за пятый день, если весь путь он прошёл за 7 дней, а расстояние между городами составляет 147 километров.',
        correct: '24', distractors: ['48', '12', '25', '23', '36'] },
    { question: 'Турист идёт из одного города в другой, каждый день проходя больше, чем в предыдущий день, на одно и то же расстояние. Известно, что за первый день турист прошёл 11 километров. Определите, сколько километров прошёл турист за седьмой день, если весь путь он прошёл за 10 дней, а расстояние между городами составляет 245 километров.',
        correct: '29', distractors: ['58', '14,5', '30', '28', '43,5'] },
    { question: 'Грузовик перевозит партию щебня массой 210 тонн, ежедневно увеличивая норму перевозки на одно и то же число тонн. Известно, что за первый день было перевезено 2 тонны щебня. Определите, сколько тонн щебня было перевезено за девятый день, если вся работа была выполнена за 14 дней.',
        correct: '18', distractors: ['36', '9', '19', '17', '27'] },
];

function shuffle<T>(arr: T[]): T[] {
    const a = [...arr];
    for (let i = a.length - 1; i > 0; i--) {
        const j = Math.floor(Math.random() * (i + 1));
        [a[i], a[j]] = [a[j], a[i]];
    }
    return a;
}

const main = async () => {
    try {
        console.log('Seeding: ЕГЭ Математика Профиль → Unit 10 → Арифметическая прогрессия');

        const [lesson] = await db.insert(schema.lessons).values({
            title: 'Арифметическая прогрессия в текстовых задачах',
            unitId: UNIT_ID,
            order: 6,
        }).returning();
        console.log(`lesson: ${lesson.id} "${lesson.title}"`);

        for (let i = 0; i < challenges.length; i++) {
            const c = challenges[i];
            const [challenge] = await db.insert(schema.challenges).values({
                lessonId: lesson.id,
                type: 'ASSIST',
                question: c.question,
                order: i + 1,
                points: 10,
                author: AUTHOR,
                difficulty: '',
                imageSrc: c.image ? `/geometry/${c.image}.svg` : '',
            }).returning();

            const options = shuffle([
                { text: c.correct, correct: true },
                ...c.distractors.map((d) => ({ text: d, correct: false })),
            ]);

            await db.insert(schema.challengeOptions).values(
                options.map((o) => ({
                    challengeId: challenge.id,
                    text: o.text,
                    correct: o.correct,
                }))
            );

            console.log(`  [${i + 1}/${challenges.length}] challenge ${challenge.id} — "${c.correct}"`);
        }

        console.log('Готово!');
    } catch (error) {
        console.error(error);
        throw new Error('Не получилось заполнить БД');
    } finally {
        await queryClient.end();
    }
};

main();
